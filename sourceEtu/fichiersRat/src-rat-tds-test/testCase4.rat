enum Mois {Janvier, Fevrier, Mars, Avril, Mai, Juin, Juillet, Aout, Septembre, Octobre, Novembre, Decembre };

test{
  int x = 3;
  int y = 4;
  int z = (x + y);
  switch (z) {
	case 7 :
		print z;
		break;
	case Lundi :
		print (z + 1);
		break;
	case true :
		print (z + 2);
		break;
	case false :
		print (z + 3);
		break;
	default :
		print x;
		break;
  }
}